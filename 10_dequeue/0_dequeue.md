## Python
```py
from collections import deque
queue = deque()
queue = deque(maxlen=10)
queue.append_left('a')
queue.pop()
queue.append('a')
queue.pop_left()
queue.clear()
queue.count(x)
queue.insert(i, x)
queue.remove(x)
```

## Ruby
- Not inbuilt
